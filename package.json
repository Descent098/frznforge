{
  "name": "frznforge",
  "private": true,
  "type": "module",
  "version": "0.4.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "comment": "Node builds nothing here. 0.4.0 made frznforge a Go binary: `go build ./cmd/frznforge` produces it, and `frznforge ingest`/`build`/`dev` replace every script this file used to carry. What is left is the Playwright suite, which is the one thing that still needs a browser and therefore Node.",
  "scripts": {
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/node": "^26.4.1"
  }
}
